!!!html
%html
  %head
    = haml :'partials/layout_head', layout: false, locals: {title: title}

  %body.layout-minimal
    .body-container
      = render_flashes unless defined?(no_flash) && no_flash
      = yield

