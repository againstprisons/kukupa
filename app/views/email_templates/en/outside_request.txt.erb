Hello,

The case for <%= e(case_obj.get_name) %>
has had a new outside request submitted.

<% if case_is_new %>
This is a new case, created by <%= e(site_name) %> along with
this outside request.
<% end %>

To view details of this case, and the new outside request,
visit: <%= e(case_url) %>
