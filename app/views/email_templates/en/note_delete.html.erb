<p>Hello,</p>

<p>
  A case note in the case for <%= e(case_obj.get_name) %><% if author %>,
  created by <em><%= e(author.decrypt(:name)) %></em>, <% end %>
  has been deleted.
</p>

<% if user %><p>
  The case note was deleted by <em><%= e(user.decrypt(:name)) %></em>.
</p><% end %>

<p>
  The contents of this case note were:
  <blockquote><%= content %></blockquote>
</p>

<p>
  To view the case this note belonged to, visit:
  <a href="<%= e(case_url) %>"><%= e(case_url) %></a>
</p>
